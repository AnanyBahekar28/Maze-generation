"""
Configure the start cell
Configure bot cell          #  bot cell will change according to the movement towards the target cell
Configure the target cell
def createMaze()):
    def createPath():
        the path will randomly generated with the random choosing of ways for the bot
        
    def createOtherPartOfMaze():
        Randomly generate the blocks
    
    createPath()
    createOtherPartOfMaze()
def findPath():
    loop until the bot has found a path using the path finding breadth first code.
        if bot cell == target cell:
            break
        else:
            continue finding path
if __name__ == "__main__":
    createMaze()
    findPath()
"""
